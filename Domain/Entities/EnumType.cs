namespace Domain.Entities
{
    public enum Type { Ping, Snmp }

    public enum Isp { Tot, True, Tbb }

    public enum Status { Dn, Up }

    public enum Office { Closed, Open }

    public enum UserStatus { Wait, Approved }
}